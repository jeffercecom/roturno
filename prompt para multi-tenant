Actúa como un Arquitecto de Software Senior y experto en bases de datos multi-tenant.

Necesito refactorizar la base de datos y la arquitectura de permisos de mi aplicación actual para convertirla en un SaaS Multi-Tenant (Multi-Organización) con aislamiento estricto de datos por cliente.

Actualmente la app solo funciona para un único negocio/Owner. Necesito agregar la cima de la pirámide jerárquica y aislar la información.

Por favor, realiza las siguientes modificaciones y dame las instrucciones paso a paso:

   - Añade el campo `organization_id` como clave foránea (FK) a TODAS las tablas existentes que almacenen datos del negocio (ej. sedes, asesores, clientes, ventas, configuraciones, etc.).

2. AISLAMIENTO Y SEGURIDAD (ROW LEVEL SECURITY - RLS / FILTROS):
   - Implementa o genera las políticas de seguridad o los middlewares necesarios para que:
     a) Un usuario con rol 'super_admin' tenga acceso global a la gestión de 'organizations' (activar/suspender negocios).
     b) Un usuario con rol 'owner' o sub-roles SOLO pueda leer, crear, actualizar o eliminar registros que pertenezcan exactamente a su `organization_id`.
     c) Un 'owner' pueda invitar/crear nuevos usuarios asignándoles automáticamente su mismo `organization_id`.

3. CONTROL DE ACCESO SEGÚN ROL DENTRO DE LA ORGANIZACIÓN:
   - Define la matriz de permisos para que el 'owner' tenga control total dentro de su organización (crear sedes, gestionar administradores, asesores)
Adicionalmente, para el registro del rol 'owner', no usaremos la API de Google ni SMS. 

Implementa el siguiente flujo de alta de Owners:
1. El Super Admin crea la Organización y el correo del Owner.
2. El sistema genera una contraseña aleatoria temporal y marca el usuario con `must_change_password = true`.
3. Al hacer login por primera vez con la contraseña temporal, la aplicación debe interceptar la sesión y exigir al Owner que cambie la contraseña antes de poder navegar o gestionar su organización.


 
Necesito refactorizar mi aplicación actual para convertirla en una plataforma Multi-Tenant (Multi-Organización) con aislamiento estricto de datos por cliente y construir el Dashboard de Super Admin que me falta.

A continuación, ejecútalo en las siguientes fases:

---

1. ESTRUCTURA DE BASE DE DATOS Y AISLAMIENTO (MULTI-TENANCY):
   - Crea las `organizations` con los campos: `id` (UUID), `name`, `status` ('active', 'suspended', 'pending'), `plan`, `created_at`, `updated_at`.
   - En todos los roles, asegura que existan los campos `organization_id` (FK a `organizations.id`) y rol
   - Añade el campo `organization_id` como clave foránea a TODAS las tablas de la app que almacenen información del negocio (sedes, admins, asesores.).
   - Implementa o genera las políticas de seguridad (RLS / Middlewares) para garantizar que:
     a) El rol 'super_admin' tenga acceso global a la gestión de 'organizations'.
     b) Un 'owner' y sus sub-roles SOLO puedan leer, crear, actualizar o eliminar registros que pertenezcan exactamente a su `organization_id`.

---

2. FLUJO DE ALTA Y CREDENCIALES DEL OWNER:
   - Agrega a la tabla de usuarios el campo booleano `must_change_password` (por defecto `true` al crearse).
   - Cuando se cree un Owner, el sistema debe generar una contraseña aleatoria temporal.
   - En el frontend, al hacer login con la contraseña temporal y detectar `must_change_password = true`, intercepta la sesión y redirige obligatoriamente a una pantalla para definir una contraseña definitiva.

---

3. DASHBOARD DE SUPER ADMIN (MÓDULO EXCLUSIVO):
   Diseña y crea el Dashboard visual para el rol 'super_admin' con la siguiente interfaz y funcionalidades (CRUD completo):

   A) Métrica y Resumen General:
      - Total de Organizaciones registradas, Organizaciones Activas, Suspendidas y Pendientes.

   B) Vista de Tabla / Lista de Organizaciones (Read):
      - Columna de Nombre de la Empresa / Organización.
      - Correo del Owner principal.
      - Estado actual (Badges visuales: Activa / Suspendida / Pendiente).
      - Fecha de creación y plan asignado.
      - Acciones rápidas en cada fila: Editar, Cambiar Estado (Activar/Suspender), Reestablecer Contraseña del Owner y Eliminar.

   C) Modal / Formulario de Creación de Organización (Create):
      - Campos: Nombre de la Organización, Nombre del Owner, Correo del Owner, Plan asignado.
      - Al guardar: Crea la fila en `organizations`, crea el usuario 'owner' vinculado con su `organization_id`, genera la contraseña temporal y activa la bandera `must_change_password = true`.

   D) Modificación y Gestión de Estado (Update / Delete):
      - Opción de "Suspender": Desactiva el acceso a todos los usuarios vinculados a esa `organization_id` inmediatamente (útil si el cliente no renueva su pago).
      - Opción de "Activar": Devuelve el acceso completo.
      - Opción de "Eliminar": Borrado lógico o físico de la organización previa confirmación.

 Muestra primero los scripts SQL o cambios de esquema de base de datos y luego el código/componentes del Dashboard de Super Admin.
